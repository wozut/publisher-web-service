package tcla.contexts.tcla.core.domain.report.model

object ClusterTexts {
    val teamCharacteristics = TextVariants(capitalized = "Team Characteristics")

    val taskCharacteristics = TextVariants(capitalized = "Task Characteristics")

    val workPracticesProcesses = TextVariants(capitalized = "Work Practices & Processes")

    val workEnvironmentTools = TextVariants(capitalized = "Work Environment & Tools")
}

